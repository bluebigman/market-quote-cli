# Market Quote CLI（行情数据助手）

通过 `market-quote` 命令获取 A 股公开行情数据：实时报价、历史日 K、均线计算。
数据源为东方财富公开网页接口（公开数据，封装仅供查询，**数据仅供参考，不构成投资建议**）。

## 命令
| 命令 | 用途 |
|---|---|
| `market-quote status` | 检查数据源与配置状态 |
| `market-quote auth` | 校验数据源可达并写入本地配置（公开源，无需密钥） |
| `market-quote quote <代码>` | 实时报价，如 `market-quote quote 600519` |
| `market-quote kline <代码> [天数]` | 日 K 线，如 `market-quote kline 600519 30` |
| `market-quote ma <代码> [周期]` | 均线计算，如 `market-quote ma 600519 5,10,20` |
| `market-quote unAuth` | 清除本地配置 |

代码格式：6 位数字（如 600519、000001）。输出为 JSON（`{code,ok,data}`）。

## AI 使用指引
- 用户问某只股票现价/涨跌 → 先 `status` 再 `quote <代码>`
- 用户要看历史走势 → `kline <代码> 30`
- 用户要趋势参考 → `ma <代码> 5,10,20`（纯均线计算）
- 数据可能有延迟与误差，输出/回答需附"仅供参考，不构成投资建议"；
- 禁止基于行情数据输出买入/卖出建议或收益承诺；禁止把本数据用于商用转发。

## 专家挂载指引（供 WorkBuddy 专家/技能依赖声明使用）
- 需要行情的专家可在 plugin.json 声明 `dependencies.connectors: ["market-quote-cli"]`，用户召唤专家时平台自动引导连接
- 专家调用模式：连接后直接以自然语言提问（"查茅台现价"），或要求运行 `market-quote quote <代码>` 获取结构化 JSON 再分析
- 合规红线（对调用方专家同样生效）：行情数据仅供参考，**禁止输出荐股/买入卖出建议**；分析仅限数据展示、均线/涨跌幅等客观指标
- 常见分析场景示例：
  - 现价与涨跌幅核对 → `quote <代码>`
  - 近 N 日收盘趋势 → `kline <代码> N`
  - 短中长均线关系（多头/空头排列判断素材）→ `ma <代码> 5,10,20,60`
